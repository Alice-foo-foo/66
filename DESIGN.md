这是一份基于当前《少女前线2：追放》官方网站（移动端视图）前端视觉与交互设计的 `DESIGN.MD` 文档。

---

# DESIGN.MD - 《少女前线2：追放》官方网站前端设计规范

## 1. 设计概述 (Design Overview)

本网站采用了“战术科幻” (Tactical Sci-Fi) 与 **“废土机能风” (Tech-wear/Wasteland)** 的设计语言，高度契合其作为二次元3D策略战棋游戏的定位。整体视觉风格偏向冷酷、硬核，大量使用高对比度的暗黑与深红配色，辅以毛刺（Glitch）、扫描线（Scanline）和 HUD 战术UI元素，营造出沉浸式的末日战场氛围。

## 2. 色彩规范 (Color Palette)

色彩系统以暗色调为主，通过高对比度的亮色进行引导和视觉冲击。

* **🌑 主背景色 (Primary Background):** `黑色 / 深灰` (如 `#0D0D0D`, `#1A1A1A`)
* 用于页面底层，提供深邃、压抑的废土世界观基调。
* **🩸 品牌/活动主题色 (Theme Accent):** `深绯红 / 暗血红` (如 `#8B0000`, `#A51C24`)
* 当前“伊利昂之围”活动的主视觉基调，用于背景特效、光晕及重要视觉分割线。
* **⚪ 主要文本色 (Primary Text):** `纯白` (`#FFFFFF`)
* 用于主要标题、按钮文字，在暗背景下保证绝对的易读性。
* **🔘 次要文本/点缀色 (Secondary Text/Decal):** `浅灰 / 战术银` (`#A0A0A0`, `#D3D3D3`)
* 用于英文副标题、背景中的机能风装饰文字（如 HUD 数据、乱码）。

## 3. 排版与字体 (Typography)

字体排版强调“机械感”与“现代感”，并大量采用中英双语混合排版（排版装饰）。

* **正文/基础 UI 字体:**
* 无衬线黑体（如 `PingFang SC`, `Microsoft YaHei`, `Helvetica Neue`）。字重常规或偏细，保持界面干净利落。
* **主视觉标题 (Display Font):**
* 特殊定制的“破坏/战损风”字体（如当前主视觉的“伊利昂之围”）。字体边缘带有不规则的撕裂、喷漆与毛刺效果，极具视觉张力。
* **排版细节:**
* **机能风边框:** 标题两侧常使用括号 `[ ]` 或十字准星 `+` 进行包裹修饰，例如 `[ GIRLS' FRONTLINE 2: EXILIUM ]`，增强战术UI感。

## 4. 布局结构 (Layout & Structure - 移动端优先)

页面采用经典的移动端单页式/大视差滚动布局，首屏极具视觉冲击力。

### 4.1 顶部导航 (Global Header)

* **左侧:** 游戏 App Icon 与两行式标题（主标题+副标题说明），尺寸适中。
* **右侧:** 汉堡菜单图标（三道杠 `≡`），线条硬朗。
* **背景:** 采用半透明的黑色磨砂玻璃效果（毛玻璃模糊），或与背景融合的渐变黑，确保滚动时不遮挡主视觉。

### 4.2 首屏主视觉 (Hero Section)

* **背景层:** 带有扫描线特效与微弱红光的氛围背景。
* **角色插画层:** 高精度的 Live2D/3D渲染角色立绘（白发与金发角色），占据屏幕核心区域，视觉重心偏中上。
* **适龄提示:** 左上角固定放置高饱和度的 CADPA `16+` 适龄提示标识，符合合规要求。

### 4.3 行动号召区 (Call to Action / CTA)

* 位于屏幕中下方，悬浮于主视觉之上。
* **主按钮 (Primary Button):**
* 超大号矩形按钮，文本为“立即下载”。
* 配色采用极其醒目的 **灰白底色 + 黑色文字**，在暗黑红背景中脱颖而出，吸引点击。右上角常伴有微小的 `DOWNLOAD` 装饰英文字母。
* **次要按钮组 (Secondary Buttons):**
* 位于主按钮下方，采用平铺或网格布局。
* 包含渠道下载入口（TapTap, Bilibili, Android/Apple Logo）。
* 采用 **半透明/暗色底 + 白色 Logo** 的弱化设计，主次分明。

## 5. 视觉特效与 UI 素材 (VFX & Assets)

前端实现中需重点关注以下 UI 细节与特效，以还原设计稿的质感：

* **Glitch & CRT 效果:** 页面背景或大标题边缘带有微弱的色彩偏移（色差畸变）和横向扫描线（Scanlines）遮罩层。
* **HUD 装饰 (Decals):** 界面四周角落、按钮边缘分布有细小的准星、刻度线、条形码或机密编号文字（如 `>>>`, `//`, `SYS.ON`），营造系统界面的真实感。
* **硬切角设计 (Chamfered Corners):** 许多 UI 容器或边框可能避免使用圆滑的 `border-radius`，而是采用切角（Clip-path）设计，以凸显机械与军事硬核感。
